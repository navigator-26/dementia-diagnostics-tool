import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dementia.diagnostics',
  appName: 'Dementia Diagnosticsool',
  webDir: 'www'
};

export default config;


